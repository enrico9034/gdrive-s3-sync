# Example configuration for MinIO self-hosted S3
# Esempio di configurazione per MinIO self-hosted

# MinIO Credentials (default for Docker MinIO)
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_REGION=us-east-1
S3_BUCKET_NAME=gdrive-sync

# MinIO Endpoint
# Local Docker: http://localhost:9000
# Remote server: https://minio.yourdomain.com
S3_ENDPOINT_URL=http://localhost:9000

# Google Drive Configuration
GDRIVE_FOLDER_ID=your_gdrive_folder_id_here
GDRIVE_CREDENTIALS_PATH=/app/credentials/credentials.json

# Sync Configuration
SYNC_INTERVAL_SECONDS=300
LOG_LEVEL=INFO
